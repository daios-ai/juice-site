# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

# Manuals fetched at build time by script/fetch-manuals.
# These live in their own repositories; never commit them here.
_juice/

# Pulled from the manual's repo by the same script; served at the site root.
install.sh

# Ruby
Gemfile.lock
vendor/
.bundle/
